import React from 'react';\nimport { PanelHeader } from './PanelHeader';\nimport { PanelContent } from './PanelContent';\n\ntype Props = {\n  container: any;\n  state: {\n    agentId: string;\n    configId?: string;\n  };\n};\n\nexport const AgentPanel: React.FC&lt;Props&gt; = ({ container, state }) =&gt; {\n  const { agentId } = state;\n\n  const handleClose = () =&gt; {\n    if (container &amp;&amp; container.remove) {\n      container.remove();\n    }\n  };\n\n  return (\n    &lt;div style={{ height: '100%', display: 'flex', flexDirection: 'column' }}&gt;\n      &lt;PanelHeader agentId={agentId} onClose={handleClose} /&gt;\n      &lt;PanelContent agentId={agentId} /&gt;\n    &lt;/div&gt;\n  );\n};\n