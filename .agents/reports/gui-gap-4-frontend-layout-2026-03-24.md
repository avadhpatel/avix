# GUI Gap 4: Golden Layout Frontend — Implementation Report\nDate: 2026-03-24\nStatus: COMPLETE\n\n## What was implemented\n\n* `crates/avix-app/src/package.json` — added `react-hot-toast` dep\n* `crates/avix-app/src/src/App.tsx` — GoldenLayout root w/ fixed +Add button, modal handler, agentPanel factory, empty init, persistence integration\n* `crates/avix-app/src/src/hooks/useLayoutPersistence.tsx` — loadLayoutConfig/saveLayout on mount/stateChanged, tauri invoke, fallback empty row\n* `crates/avix-app/src/src/components/AddAgentModal.tsx` — form (name/desc + advanced), spawn_agent invoke, toast feedback, onAdded callback\n* `crates/avix-app/src/src/components/AgentPanel.tsx` — flex col header+content, container.remove() close\n* `crates/avix-app/src/src/components/PanelHeader.tsx` — agentId display w/ summary/status placeholder, session select, close btn\n* `crates/avix-app/src/src/components/PanelContent.tsx` — agent placeholder (connect TODO)\n\n## Test results\n\nManual via `npm run dev` (localhost:5173):\n* Empty layout loads, draggable zones\n* +Add → modal form → toast (spawn fails w/o backend)\n* Mock agentAdded → adds panel w/ custom header/content/close/session\n* Drag/split panels → stateChanged → save invoke (fails w/o backend)\n* Reload → fallback empty (load fail)\n\nNo vitest setup.\n\n## Clippy\n\nN/A (TS frontend)\n\n## Remaining gaps / ignored tests\n\n* Backend: `spawn_agent({name,desc}) -> {agent_id:string}`, `load_layout() -> string|null`, `save_layout({config:string})`\n* PanelContent: real agent streams by agentId/configId\n* Multi-session: new session btn adds `${agentId}-${sessionId}` panel\n* Fetch agentName/task/status for header\n* Bell icon (notifs)\n* Vitest component snapshots\n\n## Bugs found during implementation\n\nNone.\n\n## Notes for the next agent\n\n* GoldenLayout v2 React: simple JSX factory; watch for re-mount issues on drag/popout (virtual fallback if needed)\n* Persistence: stringify(saveLayout()), assumes backend parses LayoutConfig JSON\n* Spawn assumes `{agent_id}` response shape\n* Port 5173 conflict on restart — kill prior vite\n* Dev plan spec s4 UX matched exactly.