# GUI App Gap 4: Golden Layout Frontend

## Spec Reference
section 4 Frontend.

## Tasks
1. src/App.tsx: golden-layout root (top-right +Add Agent).
2. Panel: agent-name header (task summary, status subtitle).
3. Multi-panel per agent (sessionId).
4. Layout persistence load/save (ui-layout.json).
5. Test: snapshot initial/loaded layout.

Verify: Drag/drop panels, persist.

Est: 3h.