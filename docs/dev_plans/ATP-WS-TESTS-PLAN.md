# ATP WS Tests Development Plan\n\nDate: 2024-10-25\nRef: ATP-WS-TEST-SUITE-SPEC-20241025.md, ATP-PROTOCOL-REVIEW-20241025.md\n\n## Gap Analysis\n* No existing ATP WS integration tests.\n* Unit tests cover ATP token, session, but no E2E WS connect/login/rpc/event.\n* Workspace lacks tests/ or dedicated integration crate.\n* Dev-deps missing: portpicker=0.1, tungstenite=0.24, tokio-tungstenite=0.24 (current 0.21), reqwest=0.12 (0.11).\n* Server spawn: Use `cargo run --bin avix-cli -- start --root &lt;temp&gt;` (confirm --port, --log-level trace, --init-test-data?).\n* GUI: Stubs in avix-app, may need real client-core integration.\n\n## Tasks / Gaps\n\n1. **Add dev-dependencies &amp; integration test crate** (Priority 1)\n   * Update Cargo.toml workspace.dependencies with dev-deps.\n   * Add `crates/avix-tests-integration` to workspace.members.\n   * Init Cargo.toml in crates/avix-tests-integration with tokio::test deps.\n\n2. **Implement spawn_server() util** (P1)\n   * Dynamic port (portpicker).\n   * Temp root dir (tempfile).\n   * Spawn `cargo run -p avix-cli -- start --root &lt;temp&gt; --port &lt;port&gt; --log-level trace`.\n   * Wait for /atp/health, timeout 30s.\n   * Note: Confirm if --port / --init-test-data supported; else seed data post-start.\n\n3. **Basic ATP cycle test** (P1)\n   * spawn_server() → login HTTP → WS connect Bearer → session.ready ack → proc.list RPC.\n\n4. **Full coverage** (P2)\n   * proc lifecycle (spawn/output/exit events)\n   * fs ops\n   * sys.info\n   * Errors (bad token, invalid method)\n   * Subscribe/unsubscribe\n\n5. **CI/Release gate** (P2)\n   * cargo test crates/avix-tests-integration -- --nocapture\n   * Add to cargo-make or pre-commit.\n\n## Agent Loop\n* coding-agent: impl-gap-1 (deps + crate + spawn + basic test)\n* testing-agent: verify suite, debug, logs\n* usability-agent: validate TUI/GUI use same ATP (connect flags, paths)\n* architect: refine spec if server/GUI gaps\n* Repeat until COMPLETE\n\n## Risks\n* Server flags mismatch → manual seed.\n* Version conflicts (tokio-tungstenite 0.24 vs 0.21).\n* GUI stubs block full E2E?