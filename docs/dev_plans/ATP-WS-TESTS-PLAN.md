# ATP WS Tests Development Plan\n\nDate: 2024-10-27\nRef: ATP-PROTOCOL-REVIEW-20241025.md, ATP-WS-TEST-SUITE-SPEC-20241025.md, ARCHITECT-REPORT-GAP5-REFINE-20241027.md\n\n## Status\n* Gaps 1-3: COMPLETE (spawn_server util, basic tests: login/WS/session.ready/proc.list empty, errors basic, events proc.start)\n* Gap4: PARTIAL (basic coverage; full pending test infra)\n\nCurrent tests (crates/avix-tests-integration):\n* test_basic: full cycle\n* test_errors: bad login/WS token/RPC\n* test_events: proc.start ok\n\n## Gap Analysis\n* Full ATP ops/events/errors/reconnect per protocol review.\n* Test infra for determinism (mocks, shell).\n* GUI stubs (usability).\n* CI gate.\n\n## Tasks / Gaps\n\n1. **Add dev-dependencies & integration test crate** (P1): DONE\n2. **Implement spawn_server() util** (P1): DONE (cargo run -p avix-cli --bin avix server --root &lt;temp&gt; --port &lt;dynamic&gt; --log trace, health check)\n3. **Basic ATP cycle test** (P1): DONE (login/WS connect/session.ready/proc.list)\n\n4. **Full coverage** (P1) - Deferred until Gap5 mocks\n   4.1 **Proc Lifecycle Events** (P1)\n      * proc.spawn {&quot;cmd&quot;: [&quot;echo&quot;, &quot;hi&quot;], &quot;name&quot;: &quot;test-proc&quot;} → proc.list (id/pid/status)\n      * Subscribe \&quot;*\&quot;, stdin? → proc.output\n      * proc.kill → proc.exit, gone from list\n      * proc.status, fs ops (proc.fs.read/write), sys.info {}\n   4.2 **Full Errors** (P1)\n      * bad WS token, invalid method, malformed JSON-RPC\n      * Auth expiry\n   4.3 **Basic Reconnect** (P1)\n      * Close WS, reopen token → session.ready, state preserved\n   4.4 **Additional** (P2)\n      * fs/sys standalone\n      * subscribe/unsubscribe specific\n\n5. **Test Infrastructure** (P1)\n   5.1 **Server Fixes for Tests** (P1)\n      * Proper auth validation (HMAC creds, no bypass)\n      * --test-mode: IPC mock layer\n        * Seed procs: echo \&quot;test output\&quot;, sleep 30 (emit output/exit on kill)\n        * Deterministic events: all AtpEventKind emitted periodically\n        * No real kernel/IPC, avoids Eunavail/races\n      * spawn_server() adds --test-mode\n   5.2 **CLI ATP Shell** (P1)\n      * avix atp shell --server http://host:port [--token TOKEN]\n      * No token → interactive login\n      * WS connect, auto-subscribe \&quot;*\&quot;\n      * REPL: JSON-RPC cmds (parse or raw), pretty-print replies/events\n      * Builtins: help, quit, sub/unsub, history\n   5.3 **Expand Tests** (P1)\n      * Use test-mode server\n      * Implement full Gap4 tests (proc/fs/sys/reconnect/errors/sub)\n\n6. **CI/Release gate** (P2)\n   * cargo test crates/avix-tests-integration -- --nocapture\n   * Add to cargo-make/pre-commit\n\n## Agent Loop\n* coding-agent: impl-gap 5.1 (prioritize)\n* testing-agent: verify + observability\n* usability-agent: Shell UX, TUI/GUI ATP\n* architect: Refine on feedback\n* program-manager: Track sub-gaps, Gap5 COMPLETE → Gap4 full → Gap6\n* Repeat 5.1-5.3\n\n## Risks\n* Reconnect session persistence (test w/ mocks)\n* Client/server ATP version match\n* GUI ATP stubs (usability)\n* Mitigated: IPC/kernel/races via --test-mode mocks