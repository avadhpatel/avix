# GUI App Gap 6: HIL + Notifications

## Spec Reference
section 4 HIL/notifications.

## Tasks
1. HIL: orange banner per-panel + react-hot-toast (Approve/Deny), queue in center.
2. Notifications: toast bottom-right + bell popover center (unified store).
3. Layout levels C/D/E persist (geometry/agent bind/task/focus).
4. Initial layout rules.
5. Test: toast render, persist.

Verify: HIL toast/banner, notifs center.

Est: 2h.